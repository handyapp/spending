<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.handyapp.spending">

    <application
        android:name="com.github.handyapp.spending.SpendingApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_svg"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data android:name="DATABASE" android:value="spending.db" />
        <meta-data android:name="VERSION" android:value="1" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.github.handyapp.spending.domain" />

        <activity android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AddPaymentMethodActivity" />
        <activity android:name=".activities.TransactionHistoryActivity"></activity>
    </application>

</manifest>